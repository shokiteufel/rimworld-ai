namespace RimWorldBot.Core
{
    // Placeholder — wird in Story 2.x mit Factory-basierten Kollaborateuren befüllt
    // (ISnapshotProvider, MapAnalyzer, PhaseStateMachine, EmergencyResolver, Planner etc.).
    // Story 1.3 setzt den Typ nur damit BotGameComponent.controller kompiliert.
    public class BotController
    {
    }
}
